<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <TextView
        android:id="@+id/maison_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/maison_price"
        android:layout_below="@+id/maison_type"
        android:text="descritpion"
        android:textColor="@color/black"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/maison_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/maison_description"
        android:layout_below="@+id/maison_price"
        android:text="category"
        android:textColor="@color/black"
        android:textSize="12sp" />

    <ImageView
        android:id="@+id/maison_image"
        android:layout_width="50dp"
        android:layout_height="30dp"
        android:layout_above="@+id/maison_type"
        android:layout_alignParentLeft="true"/>

    <TextView
        android:id="@+id/maison_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="22dp"
        android:layout_toRightOf="@+id/maison_image"
        android:text="prix"
        android:textColor="@color/black"
        android:textSize="20sp" />

</RelativeLayout>